import 'package:flutter/material.dart';

/// =====================
/// 색상 정의
/// =====================
class AppColors {
  // Private constructor to prevent instantiation
  AppColors._();

  // Primary Colors
  static const primary = Color(0xFF9C95CA);
  static const primaryLight = Color(0xFFA7A6CB);
  static const primaryDark = Color(0xFF8E86C9);

  // Secondary Colors
  static const secondary = Color(0xFFE56E50);
  static const secondaryLight = Color(0xFFE8A09B);

  // Accent Colors
  static const accent = Color(0xFF009DFF);
  static const accentBlue = Color(0xFF1E88FF);
  static const focusBorder = Color(0xFF5A7EFF);

  // Background Colors
  static const background = Color(0xFFF5F5F5);
  static const backgroundLight = Color(0xFFF0F4FF);
  static const backgroundBlue = Color(0xFFF2F4FB);
  static const backgroundDialog = Color(0xFFF5F6FF);
  static const cardBackground = Colors.white;

  // Text Colors
  static const textPrimary = Colors.black;
  static const textSecondary = Color(0xFF8B8B8B);
  static const textHint = Color(0xFFBDBDBD);
  static const textDisabled = Color(0xFF9E9E9E);
  static const textLight = Color(0xFFC9C9C9);

  // Border Colors
  static const border = Color(0xFFE3E3E3);
  static const borderLight = Color(0xFFE0E0E0);
  static const borderDark = Color(0xFFCFD0D1);
  static const borderGray = Color(0xFFCACACA);
  static const divider = Color(0xFFE5E5E5);
  static const dividerDark = Color(0xFFC1C1C1);

  // Button Colors
  static const buttonDisabled = Color(0xFFE6E6E6);
  static const buttonGray = Color(0xFFD2D2D2);

  // Status Colors
  static const error = Color(0xFFFF2A2A);
  static const errorLight = Color(0xFFFFBEBE);
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFC107);

  // Gradient Colors
  static const gradientStart = Color(0xFFA7A6CB);
  static const gradientEnd = Color(0xFFE56E50);

  // Icon Colors
  static const iconGray = Color(0xFFD2D2D2);
  static const iconDisabled = Color(0xFF9E9E9E);

  // Star Rating
  static const starColor = Colors.amber;
  static const starInactive = Color(0xFFBDBDBD);
}